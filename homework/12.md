# 12장 과제 – 스프링 데이터 JPA

### 과제명
**Spring Data JPA Repository와 쿼리 메서드로 회원 CRUD 구현하기**

---

### 왜 이 과제를 하는가?

실무에서 JPA를 쓴다는 건 곧 **Spring Data JPA의 Repository를 쓴다**는 뜻입니다.
Entity 설계를 아무리 잘 해도, 데이터를 저장하고 조회하는 Repository를 못 만들면 아무것도 동작하지 않습니다.

이 과제는 실무에서 가장 자주 하는 작업 — **Repository 인터페이스를 만들고 쿼리 메서드를 정의하는 것** — 을 직접 경험하는 것이 목적입니다.

### 목표
- `JpaRepository`를 상속해 기본 CRUD가 자동 제공되는 것을 확인한다.
- 메서드 이름만으로 쿼리가 생성되는 **쿼리 메서드** 기능을 직접 구현한다.
- `@Query`로 JPQL을 직접 작성하는 방법을 익힌다.

---

## 시작하기

> **12장부터는 Spring Boot 프로젝트입니다.**
> 기존 3~8장의 순수 JPA(`persistence.xml`) 방식과 다릅니다.

### 스켈레톤 코드 복사

`gracie.lee/` 디렉토리에 스켈레톤 코드가 준비되어 있습니다.
아래 명령어로 **본인 디렉토리에 복사**한 뒤 시작하세요.

```bash
# 본인 디렉토리명으로 변경하세요 (예: david, hyun.k, jayy 등)
export MY_DIR=본인디렉토리명

# 스켈레톤 복사 (build.gradle.kts, application.yml 이 없는 경우)
cp gracie.lee/build.gradle.kts ${MY_DIR}/
mkdir -p ${MY_DIR}/src/main/resources
cp gracie.lee/src/main/resources/application.yml ${MY_DIR}/src/main/resources/

# chapter_12 소스 & 테스트 복사
mkdir -p ${MY_DIR}/src/main/java/homework/chapter_12
cp -r gracie.lee/src/main/java/homework/chapter_12/* ${MY_DIR}/src/main/java/homework/chapter_12/
mkdir -p ${MY_DIR}/src/test/java/homework/chapter_12
cp -r gracie.lee/src/test/java/homework/chapter_12/* ${MY_DIR}/src/test/java/homework/chapter_12/
```

> 이미 `build.gradle.kts`와 `application.yml`이 있다면 위 두 줄은 건너뛰세요.

### 복사 후 디렉토리 구조

```
{본인디렉토리}/
├── build.gradle.kts                          ← Spring Boot 의존성 포함
├── src/main/java/homework/
│   └── chapter_12/
│       ├── Chapter12Application.java         ← Spring Boot 메인 클래스 (수정 X)
│       ├── entity/
│       │   ├── Member.java                   ← 엔티티 (수정 X)
│       │   └── Team.java                     ← 엔티티 (수정 X)
│       └── repository/
│           ├── TeamRepository.java           ← 기본 Repository (수정 X)
│           └── MemberRepository.java         ← ⭐ 여기에 메서드를 추가하세요!
├── src/main/resources/
│   └── application.yml                       ← DB/JPA 설정 (수정 X)
└── src/test/java/homework/
    └── chapter_12/
        └── Chapter12HomeworkTest.java        ← 테스트 (수정 X, 이걸 통과시키면 됨)
```

### 실행 방법

```bash
# 본인 디렉토리에서 테스트 실행
cd ${MY_DIR}
./gradlew test --tests "homework.chapter_12.Chapter12HomeworkTest"
```

---

## 0) 과제 규칙
- 스켈레톤 코드(Entity, Application, application.yml, Test)는 **수정하지 않는다**.
- `MemberRepository.java`에 Q1 쿼리 메서드를 **직접 선언**하고, Q2 메서드에는 `@Query` 어노테이션을 추가하세요.
- 테스트(`Chapter12HomeworkTest`)가 모두 통과하면 완료.

### 진행 순서
1. **테스트 코드를 먼저 읽으세요** — `Chapter12HomeworkTest.java`에서 어떤 메서드명과 파라미터가 사용되는지 확인
2. **MemberRepository에 메서드를 선언** — 테스트가 호출하는 메서드 시그니처와 정확히 일치해야 합니다
3. **테스트 실행** — `./gradlew test --tests "homework.chapter_12.Chapter12HomeworkTest"`
4. **모든 테스트 통과 = 과제 완료!**

---

## 1) 엔티티 스켈레톤 (제공됨)

### Team
| 필드 | 타입 | 제약 |
|------|------|------|
| `id` | Long (PK, GeneratedValue) | |
| `name` | String | not null |

### Member
| 필드 | 타입 | 제약 |
|------|------|------|
| `id` | Long (PK, GeneratedValue) | |
| `name` | String | not null |
| `age` | int | |
| `team` | Team | `@ManyToOne(fetch = LAZY)` |

---

## 2) 데이터 준비 (테스트에서 자동 세팅됨)

```
Team: teamA(name="teamA"), teamB(name="teamB")

Member:
  - member1(name="member1", age=10, team=teamA)
  - member2(name="member2", age=20, team=teamA)
  - member3(name="member3", age=30, team=teamB)
  - member4(name="member4", age=40, team=teamB)
```

---

## 3) 구현할 것: MemberRepository

`MemberRepository.java` 스켈레톤에 TODO로 표시된 메서드들을 **직접 선언**하세요.

> **참고**: `Chapter12HomeworkTest.java`를 열어보면 각 메서드가 어떤 파라미터로 호출되고, 어떤 결과가 기대되는지 확인할 수 있습니다.

### Q1. 쿼리 메서드 (메서드 이름으로 쿼리 생성)

Spring Data JPA는 메서드 이름을 분석해서 자동으로 쿼리를 생성합니다.
아래 요구사항에 맞는 메서드 시그니처를 `MemberRepository`에 선언하세요.

| # | 요구사항 | 반환 타입 | 빈칸을 채워 메서드를 완성하세요 |
|---|---------|----------|--------------------------|
| 1 | 이름이 정확히 일치하는 회원 목록 조회 | `List<Member>` | `findBy___(String name)` |
| 2 | 이름이 일치하고, 나이가 특정 값 **초과**인 회원 목록 | `List<Member>` | `findBy___And___(String name, int age)` |
| 3 | 이름이 특정 문자열로 **시작하는** 회원 목록 | `List<Member>` | `findBy___(String prefix)` |

#### 힌트
- Spring Data JPA 쿼리 메서드 키워드: `findBy`, `And`, `GreaterThan`, `StartingWith`
- 메서드 이름의 필드명은 **Entity의 필드명과 정확히 일치**해야 합니다
- 정확한 메서드명은 테스트 코드(`Chapter12HomeworkTest`)에서 확인할 수 있습니다

---

### Q2. @Query를 활용한 JPQL 직접 작성

쿼리 메서드 이름만으로는 표현하기 복잡한 쿼리를 `@Query`로 작성하세요.
메서드 시그니처를 선언하고, `@Query` 어노테이션에 JPQL을 직접 작성해야 합니다.

| # | 요구사항 | 메서드명 | 파라미터 |
|---|---------|---------|---------|
| 1 | 특정 **팀 이름**에 소속된 회원 목록을 조회 | `findByTeamName` | `String teamName` |
| 2 | 이름에 특정 문자열이 **포함된** 회원 목록 조회 (LIKE) | `findByNameContaining` | `String keyword` |

#### 힌트
- `@Query`와 `@Param`을 함께 사용합니다:
  ```java
  @Query("select m from Member m where ...")
  List<Member> findSomething(@Param("paramName") String paramName);
  ```
- Q2-1: `Member`의 `team` 필드를 **join**하여 팀 이름으로 필터링하세요
- Q2-2: JPQL의 `LIKE`와 `concat('%', :keyword, '%')`를 활용하세요

---

## 4) 검증

아래 명령어로 테스트를 실행하세요. **5개 테스트가 모두 통과하면 과제 완료**입니다.

```bash
./gradlew test --tests "homework.chapter_12.Chapter12HomeworkTest"
```

기대 결과:
```
Chapter12HomeworkTest > Q1-1: findByName — 이름으로 회원 조회 PASSED
Chapter12HomeworkTest > Q1-2: findByNameAndAgeGreaterThan — 이름 + 나이 초과 조건 PASSED
Chapter12HomeworkTest > Q1-3: findByNameStartingWith — 이름 접두어로 조회 PASSED
Chapter12HomeworkTest > Q2-1: findByTeamName — 팀 이름으로 회원 조회 (@Query) PASSED
Chapter12HomeworkTest > Q2-2: findByNameContaining — 이름에 키워드 포함 (@Query) PASSED
```

> **컴파일 에러가 난다면?** 테스트 코드가 호출하는 메서드명과 내가 선언한 메서드명이 정확히 일치하는지 확인하세요.

---

## 5) 과제 하며 생각해볼 것들 -> 발표자가 이야기 해보아요!

#### Q1. 쿼리 메서드 vs @Query, 실무에서 어떤 기준으로 선택할까?
- 쿼리 메서드 이름이 길어지면 가독성이 어떻게 되는가?
- 예: `findByNameAndAgeGreaterThanAndTeamNameOrderByAgeDesc` — 이게 괜찮은가?

#### Q2. Spring Data JPA가 인터페이스만으로 동작하는 원리는?
- 구현 클래스를 안 만들었는데 어떻게 실행되는가?
- 힌트: 프록시, `SimpleJpaRepository`
