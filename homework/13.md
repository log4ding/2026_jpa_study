# 13장 과제 – 웹 애플리케이션과 영속성 관리

### 과제명
**@Transactional 범위에 따른 영속성 컨텍스트 생존 범위 이해하기**

---

### 왜 이 과제를 하는가?

실무에서 JPA를 쓸 때 가장 자주 마주치는 문제 두 가지:
1. **"save() 안 불렀는데 DB가 바뀌었다"** — `@Transactional` 안의 변경 감지를 모름
2. **"아까는 됐는데 여기서는 LazyInitializationException이 터진다"** — 트랜잭션 범위 밖의 지연 로딩을 모름

이 두 문제는 **`@Transactional`이 영속성 컨텍스트의 생존 범위를 결정한다**는 것을 이해하면 해결됩니다.
이 과제는 두 상황을 직접 체험하고, 왜 그런지 설명할 수 있게 되는 것이 목적입니다.

### 목표
- `@Transactional` 범위 안/밖에서 영속성 컨텍스트의 동작 차이를 직접 체험한다.
- 변경 감지(Dirty Checking)가 동작하는 조건을 정확히 이해한다.
- 트랜잭션 밖에서 지연 로딩 시 `LazyInitializationException`이 발생하는 이유를 이해하고 해결한다.

---

## 시작하기

> **12장 과제를 먼저 완료하세요.** 13장은 12장의 Entity(`Member`, `Team`)와 Repository를 **그대로 사용**합니다.
> 12장 스켈레톤 복사가 아직이라면 [12.md](12.md)의 "스켈레톤 코드 복사" 섹션을 먼저 진행하세요.

### 스켈레톤 코드 복사

12장 스켈레톤이 이미 복사되어 있다면, **chapter_13 소스와 테스트만 추가로 복사**하세요.

```bash
# 본인 디렉토리명으로 변경하세요 (예: david, hyun.k, jayy 등)
export MY_DIR=본인디렉토리명

# chapter_13 소스 복사
mkdir -p ${MY_DIR}/src/main/java/homework/chapter_13
cp -r gracie.lee/src/main/java/homework/chapter_13/* ${MY_DIR}/src/main/java/homework/chapter_13/

# chapter_13 테스트 복사
mkdir -p ${MY_DIR}/src/test/java/homework/chapter_13
cp -r gracie.lee/src/test/java/homework/chapter_13/* ${MY_DIR}/src/test/java/homework/chapter_13/
```

### 복사 후 디렉토리 구조

```
{본인디렉토리}/
├── src/main/java/homework/
│   ├── chapter_12/                           ← 12장에서 만든 Entity, Repository (공유)
│   └── chapter_13/
│       ├── Chapter13Application.java         ← Spring Boot 메인 클래스 (수정 X)
│       └── service/
│           └── MemberService.java            ← ⭐ TODO로 표시된 4개 메서드를 구현하세요
├── src/main/resources/
│   └── application.yml                       ← OSIV=false 설정됨 (수정 X)
└── src/test/java/homework/
    └── chapter_13/
        ├── Chapter13HomeworkTest.java        ← 메인 테스트 (수정 X)
        └── Chapter13BonusTest.java           ← 보너스 테스트 (수정 X)
```

### 여러분이 구현할 것

1. `MemberService.java` — TODO로 표시된 **4개 메서드**를 구현
2. 12장에서 만든 `MemberRepository.java` — **fetch join 쿼리 메서드 1개 추가** (Scenario B-2에서 필요)

### 실행 방법

```bash
cd ${MY_DIR}

# 테스트 실행
./gradlew test --tests "homework.chapter_13.Chapter13HomeworkTest"

# 보너스 테스트
./gradlew test --tests "homework.chapter_13.Chapter13BonusTest"
```

---

## 0) 과제 규칙
- 스켈레톤 코드(Entity, Application, application.yml, Test)는 **수정하지 않는다**.
- `MemberService`의 TODO 메서드를 구현한다.
- `application.yml`의 `spring.jpa.open-in-view: false` 설정은 변경하지 않는다.

---

## 1) 엔티티 — 12장 스켈레톤을 그대로 사용

13장은 별도의 엔티티를 만들지 않습니다.
12장에서 복사한 `chapter_12/entity/` 패키지의 `Member`, `Team` 엔티티를 **그대로 사용**합니다.

> 엔티티 구조가 궁금하다면 [12.md](12.md)의 "1) 엔티티 스켈레톤" 섹션을 참고하세요.

---

## 2) 데이터 준비

테스트 코드(`@BeforeEach`)에서 아래 데이터가 **자동으로 세팅**됩니다. 직접 데이터를 넣을 필요 없습니다.

```
Team: teamA(name="teamA")
Member: member1(name="member1", age=20, team=teamA)
```

---

## 3) 구현할 것

`MemberService.java`를 열면 TODO가 표시된 4개 메서드가 있습니다. 스켈레톤의 주석을 읽고 구현하세요.

### Scenario A: 변경 감지 (Dirty Checking)

| 메서드 | 핵심 포인트 |
|--------|-----------|
| `changeName` | `@Transactional` O — `save()` 없이 이름을 변경하세요 |
| `changeNameWithoutTransaction` | `@Transactional` X — 위와 동일한 로직을 작성하세요 |

#### 생각해볼 것
- A-1은 `save()`를 안 불렀는데 **DB에 반영되는가?**
- A-2는 동일한 로직인데 **DB에 반영되지 않는가?**
- 두 결과가 다른 이유는?

---

### Scenario B: 트랜잭션 밖 지연 로딩

| 메서드 | 핵심 포인트 |
|--------|-----------|
| `findMember` | Member만 조회하여 반환 (Team은 LAZY 상태 유지) |
| `findMemberWithTeam` | Member + Team을 **fetch join**으로 함께 조회하여 반환 |

> B-2를 위해 `MemberRepository`에 fetch join 쿼리 메서드를 **1개 추가**해야 합니다.

#### 생각해볼 것
- B-1에서 반환된 Member의 `getTeam().getName()`을 호출하면 **`LazyInitializationException`이 발생하는가?**
- B-2에서는 같은 호출이 **정상 동작하는가?**
- 왜 차이가 나는가?

---

### (보너스) Scenario C: OSIV로 해결하기

`Chapter13BonusTest`를 실행하면, OSIV를 `true`로 오버라이드한 상태에서 B-1의 `findMember()`를 다시 호출합니다.
이번에는 `LazyInitializationException`이 **발생하지 않습니다**. 왜 그런지 생각해보세요.

- OSIV=true일 때 영속성 컨텍스트가 어디까지 살아있는가?
- OSIV=true가 편리하지만 실무에서 끄는 이유는?

---

## 4) 검증

구현을 마쳤다면 아래 명령어로 테스트를 실행하세요. **4개 테스트가 모두 통과하면 과제 완료**입니다.

```bash
./gradlew test --tests "homework.chapter_13.Chapter13HomeworkTest"
```

기대 결과:
```
Chapter13HomeworkTest > A-1: @Transactional O → save() 없이도 UPDATE 반영 PASSED
Chapter13HomeworkTest > A-2: @Transactional X → UPDATE 미반영 PASSED
Chapter13HomeworkTest > B-1: findMember → 트랜잭션 밖에서 Lazy 접근 시 LazyInitializationException PASSED
Chapter13HomeworkTest > B-2: findMemberWithTeam → fetch join으로 Lazy 문제 해결 PASSED
```

> 테스트가 통과했다면, 각 Scenario의 "생각해볼 것"을 다시 읽고 **왜 이런 결과가 나오는지** 정리해보세요.

보너스:
```bash
./gradlew test --tests "homework.chapter_13.Chapter13BonusTest"
```

---

## 5) 과제 하며 생각해볼 것들 -> 발표자가 이야기 해보아요!

#### Q1. @Transactional이 영속성 컨텍스트의 생명주기를 결정한다는 것은 무슨 의미인가?
- 트랜잭션 시작 = 영속성 컨텍스트 생성
- 트랜잭션 커밋 = flush + 영속성 컨텍스트 종료
- 이 범위 밖에서 엔티티는 어떤 상태가 되는가?

#### Q2. 변경 감지 vs merge(), 실무에서는 어떤 것을 써야 하나?
- `merge()`의 위험성: 모든 필드를 덮어씌운다는 것은 무슨 의미인가?
- 예: 이름만 바꾸려고 `merge()`를 호출했는데 다른 필드가 null로 덮어씌워진다면?

#### Q3. fetch join 말고 다른 해결 방법은 없나?
- `@EntityGraph` 활용
- DTO로 직접 조회 (`new` 키워드 사용 JPQL)
- 각각의 장단점은?

#### Q4. OSIV=true가 편리한데 실무에서는 왜 끄는가?
- OSIV=true이면 영속성 컨텍스트가 API 응답이 끝날 때까지 살아있다 — DB 커넥션도 그때까지 반환되지 않는다면?
- 트래픽이 많은 서비스에서 커넥션 풀이 고갈되면 어떤 일이 벌어지는가?
- 컨트롤러/뷰에서 의도치 않게 지연 로딩 쿼리가 나가면 성능 문제를 추적하기 어려운 이유는?

#### Q5. 반대로, OSIV=true가 적합한 경우는?
- 트래픽이 적은 어드민/백오피스 시스템이라면?
- 빠르게 프로토타이핑해야 하는 초기 단계라면?
- OSIV를 끄면 모든 지연 로딩을 fetch join이나 DTO로 해결해야 하는데, 그 비용 대비 효과가 항상 맞는가?
